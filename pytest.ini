[pytest]
# Exclude temporary tools and common noise directories from test discovery
addopts = --ignore=temp_tools -ra
norecursedirs = temp_tools .git .venv node_modules dist build cache artifacts .specify
